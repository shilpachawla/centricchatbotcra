{
  "private": true,
  "workspaces": [
    "client",
    "Server/*"
  ],
  "scripts": {
    "build:client": "yarn workspace client build",
    "build:Server": "yarn workspace Server build"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "dotenv": "^16.3.1",
    "hnswlib-node": "^2.0.0"
  }
}
